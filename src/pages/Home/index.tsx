import React from 'react';
import MapView from 'react-native-maps';

import { Container, Map } from './styles';

const Home: React.FC = () => {
  return (
    <Container>
      <Map />
    </Container>
  );
};

export default Home;
